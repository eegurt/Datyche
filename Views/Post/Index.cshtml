@model IEnumerable<Datyche.Models.PostViewModel>

@{
    ViewData["Title"] = "Posts";
}

<p>
    <a asp-controller="Post" asp-action="Create" class="btn btn-success">Create New</a>
</p>

<table class="table">
<tbody>
<thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            Date published
        </th>
    </tr>
</thead>
@foreach (var item in Model) {
    <tr>
        <td>
            <a asp-action="Details" asp-route-id="@item.Id">@item.Title</a>
        </td>
        <td>
            @item.Author
        </td>
        <td>
            @item.DateCreated.ToLocalTime()
        </td>
    </tr>
}
</tbody>
</table>
